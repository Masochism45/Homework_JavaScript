let age = 25

if (typeof age === "number"){
    console.log("The variable 'age' is a number. ") // << ขึ้นข้อความนี้เพราะว่า Typeof แล้วค่าจะแสดงออกมาในรูปแบบ type ซึ่งเป็นข้อความ number เลยเข้าเงื่อนไขเป็น number === number
}